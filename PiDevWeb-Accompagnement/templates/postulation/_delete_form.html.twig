<form method="post" action="{{ path('app_postulation_delete', {'id_postulation': postulation.id_postulation}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ postulation.id_postulation) }}">
    <button class="btn">Delete</button>
</form>
