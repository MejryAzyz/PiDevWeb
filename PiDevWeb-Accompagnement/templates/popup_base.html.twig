<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <title>{% block title %}Popup{% endblock %}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="{{ asset('coderthemes.com/osen/layouts/assets/css/vendor.min.css') }}" rel="stylesheet">
    <link href="{{ asset('coderthemes.com/osen/layouts/assets/css/app.min.css') }}" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" rel="stylesheet" />

    <style>
        /* Styles comme dans ton exemple original */
        body.popup-mode {
            font-family: 'Inter', sans-serif;
            background: radial-gradient(circle at top left, #f1f3f8, #dbe3ea);
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            margin: 0;
        }
        .popup-content-wrapper {
            background: white;
            padding: 30px;
            border-radius: 20px;
            box-shadow: 0 12px 30px rgba(0, 0, 0, 0.15);
            max-width: 520px;
            width: 100%;
            animation: zoomIn 0.3s ease-out;
        }
        @keyframes zoomIn {
            from { opacity: 0; transform: scale(0.95); }
            to { opacity: 1; transform: scale(1); }
        }
    </style>
</head>
<body class="popup-mode">
<div class="popup-content-wrapper">
    {% block popup_content '' %}
</div>
</body>
</html>