<form method="post" action="{{ path('app_paiement_delete', {'id_paiement': paiement.id_paiement}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ paiement.id_paiement) }}">
    <button class="btn">Delete</button>
</form>
