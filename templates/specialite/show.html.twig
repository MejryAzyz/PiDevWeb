{% extends 'base.html.twig' %}

{% block title %}Specialite{% endblock %}

{% block body %}
    <h1>Specialite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id_specialite</th>
                <td>{{ specialite.idSpecialite }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ specialite.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_specialite_index') }}">back to list</a>

    <a href="{{ path('app_specialite_edit', {'id_specialite': specialite.id_specialite}) }}">edit</a>

    {{ include('specialite/_delete_form.html.twig') }}
{% endblock %}
