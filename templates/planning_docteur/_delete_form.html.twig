<form method="post" action="{{ path('app_planning_docteur_delete', {'idPlanning': planning_docteur.idPlanning}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ planning_docteur.idPlanning) }}">
    <button class="btn btn-danger btn-sm">
        <i class="ti ti-trash"></i>
    </button>
</form>
