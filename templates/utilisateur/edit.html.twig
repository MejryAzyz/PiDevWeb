{% extends 'base.html.twig' %}

{% block title %}Edit Utilisateur{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header border-bottom border-dashed d-flex align-items-center">
                    <h4 class="header-title">Modifier un Utilisateur</h4>
                </div>

                <div class="card-body">
                    <p class="text-muted">
                        Modifiez les informations de l'utilisateur ci-dessous.
                    </p>

                    {{ include('utilisateur/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

                    <div class="mt-3">
                        <a href="{{ path('app_utilisateur_index') }}" class="btn btn-secondary">Retour à la liste</a>
                    </div>
                                     <div class="mt-3" >      {{ include('utilisateur/_delete_form.html.twig') }}</div>

                </div> <!-- end card-body -->
            </div> <!-- end card -->
        </div> <!-- end col -->
    </div> <!-- end row -->
{% endblock %}
